<% for(let i=0; i < music.length; i++) { %>
    <div class="musicList">
        <h1><%= i + 1 %>: <%= music[i].songName %></h1>
        <h3>Year: <%= music[i].songDate %></h3>
        <h3>
            <iframe width="360" height="315" 
            src="https://www.youtube.com/embed/<%=music[i].songLink%>" 
            frameborder="0" 
            allowfullscreen>
    </iframe>
       </h3>
    </div>
<% } %>